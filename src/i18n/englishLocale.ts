import { createTranslator, simulatorEnglish } from './catalog.js';
/** Default English labels for pure helpers outside a React provider. */
export const englishLocale = createTranslator(simulatorEnglish);
